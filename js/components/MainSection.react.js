/**
 * @jsx React.DOM
 */

var React = require('react');

var MainSection = React.createClass({

    render: function() {
        return (
            <div />
        );
    }

});

module.exports = MainSection;
